# Katana — web crawler that feeds URLs into ffuf/nuclei. JSON output
# via -jc keeps the downstream parser pipeline simple.
FROM projectdiscovery/katana:latest
USER 1001
ENTRYPOINT ["/usr/local/bin/katana"]
