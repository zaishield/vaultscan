# Subfinder — ProjectDiscovery passive subdomain enumerator. Used by
# external_discovery profile. Produces JSONL on stdout with -oJ.
FROM projectdiscovery/subfinder:latest
USER 1001
ENTRYPOINT ["/usr/local/bin/subfinder"]
